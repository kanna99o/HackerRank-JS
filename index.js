/*
    if(N % 2 === 0){
        let current = 1;
        while(answer.length < N){
            answer.push(current);
            answer.push(-current);
            current++;
        }
    } else {
        let current = 1;
        while(answer.length < (N-1)){
            answer.push(current);
            answer.push(-current);
            current++;
        }
        answer.push(0);
    }
    */